{
  "name": "dashboard-wp-cli",
  "version": "1.1.1",
  "description": "WP-CLI Dashboard Plugin for WordPress",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:zip": "node scripts/build-zip.js"
  },
  "author": "",
  "license": "GPL-2.0-or-later",
  "devDependencies": {
    "@wordpress/env": "^10.29.0"
  }
}
